apiVersion: networking.istio.io/v1alpha3
kind: ServiceEntry
metadata:
  name:  heremap
spec:
  trafficPolicy:
    tls:
      mode: SIMPLE
  hosts:
  - reverse.geocoder.api.here.com
  ports:
  - number: 443
    name: https
    protocol: HTTPS
  resolution: DNS
  location: MESH_EXTERNAL